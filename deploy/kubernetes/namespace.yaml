# Kubernetes Namespace for FirstLight
# Deploy: kubectl apply -f namespace.yaml

apiVersion: v1
kind: Namespace
metadata:
  name: firstlight
  labels:
    app: firstlight
    environment: production
  annotations:
    description: "Geospatial event intelligence platform"
