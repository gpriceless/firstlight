# Kubernetes Ingress for FirstLight API
# Deploy: kubectl apply -f ingress.yaml
# Requires: nginx-ingress controller or similar

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: firstlight-ingress
  namespace: firstlight
  labels:
    app: firstlight
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.firstlight.example.com
      secretName: firstlight-tls
  rules:
    - host: api.firstlight.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: firstlight-api
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: firstlight-api
                port:
                  number: 80
          - path: /health
            pathType: Exact
            backend:
              service:
                name: firstlight-api
                port:
                  number: 80
